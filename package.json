{
  "name": "logistics-analytics-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.app.json && tsc -p tsconfig.worker.json && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run typecheck && npm test && npm run build",
    "smoke": "node scripts/smoke-local.ts",
    "cf:typegen": "wrangler types",
    "cf-typegen": "wrangler types",
    "data:import": "node scripts/import-data.ts",
    "db:migrate": "wrangler d1 migrations apply DB --local",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "db:seed": "wrangler d1 execute DB --local --file .generated/seed.sql",
    "db:seed:local": "wrangler d1 execute DB --local --file .generated/seed.sql",
    "db:seed:remote": "wrangler d1 execute DB --remote --file .generated/seed.sql",
    "eval:ai:live": "node scripts/eval-ai-live.ts",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@fontsource-variable/geist": "5.3.0",
    "@tanstack/react-table": "8.21.3",
    "hono": "4.13.7",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "recharts": "3.10.1",
    "zod": "4.6.4"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.54.8",
    "@types/node": "22.20.2",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "@vitejs/plugin-react": "6.1.1",
    "csv-parse": "7.0.2",
    "jsdom": "30.0.1",
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vitest": "4.1.11",
    "wrangler": "4.131.1"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260911.1": true
  }
}
